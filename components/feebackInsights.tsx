// import { getFeedbacks } from '@/index';
// import FeedbackAnalysisClient from './feedbackAnalysisComponent';

// interface FeedbackInsightsProps {
//   projectId: number;
// }

// export default async function FeedbackInsights({ projectId }: FeedbackInsightsProps) {
  
//   const feedbacks = await getFeedbacks(projectId);
//   const first10Feedbacks = feedbacks.slice(0, 10);
  
//   return <FeedbackAnalysisClient initialFeedbacks={first10Feedbacks} />;
// }